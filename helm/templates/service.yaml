apiVersion: v1
kind: Service
metadata:
  name: {{ include "teams-relay.fullname" . }}
  labels:
    {{- include "teams-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.internalPort }}
      targetPort: internal
      protocol: TCP
      name: internal
  selector:
    {{- include "teams-relay.selectorLabels" . | nindent 4 }}
